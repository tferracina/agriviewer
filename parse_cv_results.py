## Parse the results from the CV model

## We can imagine that the results are stored in a JSON object 'cv_results.json',  always in same format
## Extract the information into a pandas DataFrame

import pandas as pd
import llama_index as ll # use llamaparse

import json
